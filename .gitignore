# Terraform state
infra/terraform.tfstate
infra/terraform.tfstate.backup

# Terraform provider plugins and internals
infra/.terraform/

# Lock file (optional to commit, but fine to exclude)
# infra/.terraform.lock.hcl

#lambda build artifact
lambda.zip

#contains personal email for alerts
*.tfvars

# Node.js dependencies — restored with npm install
node_modules/

# TypeScript compiled output — generated artifact
dist/